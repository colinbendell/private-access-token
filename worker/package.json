{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "A simple PrivateToken auth scheme challenge",
  "main": "index.js",
  "author": "Colin Bendell <colin@bendell.ca>",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "wrangler": "^2.18.0",
    "@noble/curves": "^1.0.0",
    "@noble/hashes": "^1.0.0"
  },
  "scripts": {
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "wrangler publish index.js --dry-run --outdir dist  --latest"
  },
  "devDependencies": {
    "esbuild": "^0.17.18",
    "miniflare": "^2.13.0",
    "prettier": "^2"
  }
}
